{
  "name": "wasmcolors",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-wasm": "wasm-pack build --target web --release",
    "optimize-build": "wasm-opt -Oz -o ./pkg/wasmcolors_bg.min.wasm ./pkg/wasmcolors_bg.wasm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "wasm-pack": "^0.12.1"
  }
}
